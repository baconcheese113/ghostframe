"""Pipeline orchestration — fast lane and deep lane.

Composes the modules into end-to-end analysis flows. Both the CLI and API
call these functions — keeps orchestration logic out of interface layers.
"""
